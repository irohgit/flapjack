extends Node

# This Enemy does not fire any projectiles and instead moves in a pattern and must be dodged.
# They have low health, the move fast and they only do damage if more than 3 of them hit or something like that
# The enter the screen and then exit. Once exit the objects are destroyed.


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
